language: python
sudo: false

cache:
  - pip
  - apt

addons:
  apt:
    packages:
      - build-essential
      - python-dev
      - libc6
      - g++
      - gfortran
      - libhdf5-serial-dev

python:
  - 2.7
  # - 3.3
  # - 3.4
  # - 3.5

install: pip install -r requirements.txt

script: py.test .
